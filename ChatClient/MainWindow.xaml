<Window x:Class="ChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatClient"
        mc:Ignorable="d"
        Title="Chat Client" Height="450" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Username:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="txtUsername" Width="150" Margin="0,0,10,0"/>
            <Button x:Name="btnConnect" Content="Connect" Width="80" Click="btnConnect_Click"/>
        </StackPanel>

        <TextBox x:Name="txtChatBox" Grid.Row="1" Margin="10" 
                 IsReadOnly="True" TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto"/>

        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtMessage" Grid.Column="0" Margin="0,0,10,0" 
                     KeyDown="txtMessage_KeyDown"/>
            <Button x:Name="btnSend" Grid.Column="1" Content="Send" Width="80" 
                    Click="btnSend_Click" IsEnabled="False"/>
        </Grid>
    </Grid>
</Window>